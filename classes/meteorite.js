export default class Meteorite {
  constructor(speed, x, y, width, height, node) {
    this.speed = speed;
    this.x = x;
    this.y = y;
    this.width = width;
    this.height = height;
  }
  setSpeed(speed) {}
}
